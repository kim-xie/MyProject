<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"      
 "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">
<mapper namespace="com.king.dao.ContentMapper">
	<select id="findAll" resultMap="contentResultMap">
			select * from content
	</select>
	<select id="findById" parameterType="integer" resultMap="contentResultMap">
			select * from content where id=#{id}
	</select>
	<insert id="save" parameterType="content">
			insert into content(content,title,create_time,tag) values(#{content},#{title},#{createTime},#{tag}) 
	</insert>
	<update id="update" parameterType="content">
			update content set content=#{content},title=#{title} where id=#{id}
	</update>
	<delete id="delete" parameterType="integer">
			delete from content where id=#{id}
	</delete>
	
	<!-- 当实体类与字段不一致是使用 -->
	<resultMap type="com.king.bean.Content" id="contentResultMap">
	    <id property="id" column="id"/>
	    <result property="content" column="content"/>
	    <result property="title" column="title"/>
	    <result property="pic" column="pic"/>
	    <result property="createTime" column="create_time"/>
	    <result property="updateTime" column="update_time"/>
    </resultMap>
	

</mapper>